// data operations
function deleteEmployee(pId){
    alert(` sileriiim ${pId}`);
    // silme islemi, tbd
    render(yeniArray);
}

function addEmployee(){
    alert(` sileriiim ${pId}`);
    // obje olusturma islemi, tbd
    employeeList.push(yeniobje)
    render(employeeList);
}

function sortByFirstName(){
    employeeList = employeeList.sort(function(emp){});
    render(employeeList);
}